# 当前项目完善清单

> 生成日期：2026-05-23  
> 项目类型：VuePress 2 + vuepress-theme-hope 中文技术博客  
> 适用范围：内容建设、站点工程、SEO、部署、自动化脚本与协作规范

## 1. 项目现状

当前仓库是一个个人技术博客，使用 VuePress 2.0 RC 版本、Vite 打包器和 `vuepress-theme-hope` 主题。内容集中在 `docs/posts/`，主题配置位于 `docs/.vuepress/config.js`，静态资源位于 `docs/.vuepress/public/`。

已具备的能力：

- 中文站点基础配置：`lang: zh-CN`、站点标题、描述、作者、导航栏、侧边栏。
- 博客主题能力：`vuepress-theme-hope` 的博客模式、阅读时间、面包屑、页面信息。
- Markdown 增强：GFM、任务列表、tabs、code tabs、上下标、标记等。
- 自动化脚本：新文章创建、图片扫描、图片上传、图片链接替换。
- GitHub Pages 部署：`.github/workflows/deploy.yml` 已配置构建和部署流程。
- 内容结构：已建立 Java、数据库、算法、前端、后端、AI、系统技术等栏目。

当前主要风险和缺口：

- 部分导航链接指向的目录或文章尚未完善，例如 `database/es`、`ai/gemini`、`ai/claude` 等路径存在空目录或缺少对应文章。
- README、使用说明、优化说明等根目录文档存在，但缺少一份面向维护者的统一治理清单。
- SEO 配置已有基础 meta，但 sitemap、robots、结构化数据、文章摘要规范、封面图规范仍需系统化。
- 文章 frontmatter 规范未集中固化，后续大量写作时容易出现分类、标签、日期格式不一致。
- 图片处理脚本已有雏形，但缺少发布前检查流程，容易出现本地图片引用、缺失图片、远程图床配置不完整等问题。
- VuePress 2 和主题依赖仍是 RC 版本，升级策略和回归验证需要明确。

## 2. 已安装的适配技能

已通过 `npx skills find` 分析当前项目栈，并安装以下技能：

| 技能 | 来源 | 适配原因 | 状态 |
| --- | --- | --- | --- |
| `vite` | `onmax/nuxt-skills@vite` | 当前项目使用 `@vuepress/bundler-vite`，该技能可辅助处理 Vite 构建、性能和配置问题。 | 已安装 |
| `frontend-vue-development` | `tencentblueking/bk-ci@frontend-vue-development` | 当前项目基于 VuePress 和 Vue 生态，后续主题配置、页面组件、交互增强可参考该技能。 | 已安装 |
| `gh-pages-deploy` | `aviz85/claude-skills-library@gh-pages-deploy` | 当前项目通过 GitHub Pages 发布，适合辅助检查部署流程、产物路径和 Pages 配置。 | 已安装 |

搜索到但未安装的候选：

| 技能 | 原因 | 建议 |
| --- | --- | --- |
| `onmax/nuxt-skills@nuxt-seo` | 安装量较高，但领域偏 Nuxt，不是 VuePress 专属 SEO 技能。 | 不作为默认技能安装，SEO 优化优先按 VuePress 和主题能力落地。 |
| 通用 `document` 类技能 | 搜索结果安装量较低，来源可信度不足。 | 暂不安装，当前项目文档规范直接在仓库内沉淀。 |

注意：新安装的技能通常需要重启 Codex 后才能被完整加载。

## 3. 优先完善项

### 标杆博客观察

参考对象选择了几类成熟技术站点：个人技术博客、工程团队博客、产品技术博客、课程型知识站。它们不一定都使用 VuePress，但信息架构和内容运营方法可以迁移到当前项目。

| 标杆类型 | 代表站点 | 可借鉴点 | 对当前项目的启发 |
| --- | --- | --- | --- |
| 深度个人技术博客 | Martin Fowler | 长文沉淀、系列化专题、清晰的文章修订时间、稳定链接。 | JVM、并发、数据库系列应做成长线专题，文章需要标注适用版本和修订记录。 |
| 中文个人技术周刊/博客 | 阮一峰的网络日志 | 周期性内容、归档清晰、RSS 订阅、标题稳定、链接传播友好。 | 可增加“周刊/读书/实践记录”轻量栏目，提升更新频率和复访。 |
| 工程团队博客 | GitHub Blog、Netflix TechBlog、Stripe Engineering | 分类明确、案例导向、配图规范、作者信息、相关文章推荐。 | 文章可以从“知识点讲解”升级为“问题背景 -> 排查过程 -> 方案 -> 复盘”。 |
| 产品型技术博客 | Vercel Blog | 首页信息密度高、标签体系清晰、内容与框架生态连接强。 | 前端、AI、部署相关内容应强化实践入口，减少纯概念堆叠。 |
| 课程型技术知识站 | web.dev、MDN Learn | 学习路径、难度分级、前置知识、下一步阅读。 | 每个栏目 README 应承担“学习路线页”的职责，而不只是目录页。 |

可迁移的共同模式：

- 入口分层：顶部导航只保留高频入口，具体知识点交给栏目页和侧边栏。
- 内容分级：区分“入门”“进阶”“实战”“原理”“排障”。
- 系列页优先：成熟技术博客通常不只依靠标签归档，而是提供专题页承载阅读顺序。
- 文章元信息完整：发布日期、更新日期、作者、分类、标签、摘要、相关链接。
- 链接稳定：对外传播的文章 URL 尽量避免频繁重命名。
- 搜索和订阅可用：站内搜索、RSS、sitemap、robots 是长期博客的基础设施。
- 内容运营节奏明确：深度文章负责权威性，短笔记或周刊负责持续更新。

### P0：信息架构重塑

目标：让访问者进入站点后能快速判断“这里有什么、应该从哪里开始读、下一篇读什么”。

建议结构：

| 层级 | 作用 | 当前改进方向 |
| --- | --- | --- |
| 首页 | 展示站点定位、核心专题、最近更新、推荐阅读。 | 保留 BlogHome，同时补足项目卡片和推荐专题。 |
| 顶部导航 | 提供一级主题入口。 | 控制数量，避免重复项和空目录入口。 |
| 栏目 README | 作为学习路径页。 | 每个一级栏目补充“适合谁读、前置知识、阅读顺序、系列文章”。 |
| 系列专题页 | 聚合一组强相关文章。 | 为 JVM、并发、MySQL、Redis、Spring 建立专题索引。 |
| 文章页 | 解决一个明确问题。 | 增加摘要、适用版本、前置知识、延伸阅读、修订记录。 |

待办：

- 将 `docs/posts/java/`、`docs/posts/database/`、`docs/posts/ai/` 设计为一级学习路径页。
- 为 JVM、并发编程、MySQL、Redis 各建立一个专题索引页。
- 首页项目卡片优先展示“可连续阅读的专题”，而不是零散栏目。
- 导航栏避免指向空目录；新栏目上线前先补齐 README。
- 为每篇系列文章增加“上一篇 / 下一篇 / 本系列目录”链接。

验收标准：

- 新读者从首页进入后，3 次点击内能找到一个完整学习路线。
- 每个一级栏目都有明确的阅读顺序。
- 空栏目不会作为主要入口展示。

### P0：文章结构模板升级

目标：让文章从“笔记记录”升级为“可搜索、可复习、可传播的技术内容”。

建议正文结构：

```markdown
---
title: 文章标题
date: 2026-05-23
category:
  - Java
  - JVM
tag:
  - 类加载
  - 双亲委派
description: 用 60 到 120 字说明本文解决的问题和读者收益。
---

# 文章标题

## 适用范围

- JDK 版本：
- 框架版本：
- 阅读前置：

## 问题背景

## 核心结论

## 原理拆解

## 示例或实践

## 常见误区

## 面试追问

## 延伸阅读

## 修订记录
```

待办：

- 将 `create-post.js` 的模板升级为上述结构。
- 为 JVM、并发、MySQL 等深度文章补充“适用范围”和“核心结论”。
- 对实践类文章补充“环境版本”和“复现步骤”。
- 对原理类文章补充“常见误区”和“面试追问”。
- 为历史文章逐步补充修订记录，先从访问量或价值最高的文章开始。

验收标准：

- 新文章默认具备摘要、范围、结论、延伸阅读。
- 读者无需通读全文，也能通过“核心结论”快速判断是否继续阅读。
- 文章被搜索引擎摘取时有稳定描述。

### P0：发布稳定性

目标：确保每次推送到 `main` 后站点可以稳定构建并正确发布。

待办：

- 固化本地构建命令：发布前必须运行 `npm run docs:build`。
- 检查 GitHub Actions 使用的 Node.js 版本是否与本地开发一致。
- 确认 `docs/.vuepress/dist/CNAME` 的域名写入逻辑符合实际域名策略。
- 为部署 workflow 增加并发、缓存、构建失败排查说明。
- 在 README 中补充“本地构建成功后再合并到 main”的维护约定。

验收标准：

- `npm run docs:build` 本地可稳定通过。
- GitHub Pages workflow 能完整执行 `build -> deploy`。
- 构建产物路径始终为 `docs/.vuepress/dist`。

### P0：导航与目录一致性

目标：避免导航链接打开后出现空栏目、404 或内容不匹配。

待办：

- 核对 `docs/.vuepress/config.js` 中 `navbar` 的所有链接。
- 为以下路径补齐 `README.md` 或首篇文章：
  - `docs/posts/database/es/`
  - `docs/posts/ai/gemini/`
  - `docs/posts/ai/claude/`
  - `docs/posts/java/`
  - `docs/posts/database/`
- 修正重复或误导性的导航项，例如“Java 设计模式”目前指向 `java/base`。
- 为每个一级栏目提供栏目页说明：学习路径、推荐阅读顺序、已完成文章、计划文章。

验收标准：

- 首页、导航栏、侧边栏中的所有链接都能打开。
- 空目录不出现在导航或侧边栏中，除非已有明确的占位说明。
- 栏目页能解释该栏目覆盖范围和阅读路径。

### P1：文章 frontmatter 规范

目标：让博客文章在主题、归档、分类、标签、SEO 中表现一致。

建议标准：

```yaml
---
title: 文章标题
date: 2026-05-23
category:
  - Java
  - 并发编程
tag:
  - JMM
  - 多线程
  - JVM
description: 一句话概括文章解决的问题，建议 60 到 120 字。
---
```

待办：

- 更新 `create-post.js`，让新文章模板包含 `description`。
- 对既有文章补齐 `description`。
- 统一分类名称，例如 `数据库`、`MySQL`、`Redis`、`Java`、`JVM`、`并发编程`。
- 控制标签粒度，避免同义标签重复，例如 `高并发`、`并发`、`多线程` 需明确使用边界。
- 增加发布前检查脚本，扫描缺少 title、date、category、tag、description 的文章。

验收标准：

- 所有正式文章都有完整 frontmatter。
- 分类和标签在归档页面中语义清晰，不出现明显重复项。
- 新文章通过脚本生成后无需手动补齐基础元信息。

### P1：内容建设路线

目标：把现有知识点从“文章集合”提升为“可持续扩展的知识库”。

建议按系列补齐：

| 栏目 | 当前情况 | 建议补齐 |
| --- | --- | --- |
| Java 基础 | 栏目存在，内容较少 | 集合、泛型、异常、IO/NIO、反射、注解、SPI |
| JVM | 已有类加载、内存、对象、GC 系列 | JVM 参数调优、线上问题排查、Arthas、JFR |
| 并发编程 | 已有 JMM、synchronized | volatile、CAS、AQS、线程池、CompletableFuture、并发容器 |
| Spring | 栏目存在，内容待补 | IoC、AOP、事务、Spring MVC、Spring Boot 自动配置 |
| MySQL | 已有锁相关内容 | 索引、事务、MVCC、执行计划、慢 SQL 优化、主从复制 |
| Redis | 已有 Bloom Filter | 数据结构、持久化、缓存一致性、分布式锁、集群 |
| 算法 | 栏目存在，内容待补 | 数组、链表、二叉树、动态规划、回溯、图论 |
| AI | 栏目和子目录存在 | 工具链使用、Prompt 模板、AI 编程实践、模型对比 |

验收标准：

- 每个核心栏目至少有 1 篇总览和 3 篇正文。
- 系列文章标题格式统一。
- 栏目 README 提供推荐阅读顺序。

### P1：专题化运营计划

目标：参照成熟技术博客的专题运营方式，把内容建设从“想到哪写到哪”改成“按路线补齐”。

建议先做 5 个专题：

| 专题 | 目标读者 | 首批文章 |
| --- | --- | --- |
| JVM 原理与排障 | 有 Java 基础，希望理解运行时和线上问题的人 | 类加载、内存区域、对象分配、GC、GC 日志、Arthas 排障 |
| Java 并发编程 | 准备面试或处理高并发业务的人 | JMM、volatile、synchronized、CAS、AQS、线程池、CompletableFuture |
| MySQL 事务与性能 | 后端开发和数据库优化学习者 | 索引、事务、锁、MVCC、执行计划、慢 SQL、主从复制 |
| Redis 缓存实战 | 做缓存、限流、分布式锁的人 | 数据结构、持久化、缓存击穿、缓存一致性、分布式锁、布隆过滤器 |
| Spring Boot 实战 | 希望系统理解 Spring 生态的人 | IoC、AOP、事务、自动配置、Web MVC、配置加载、常见坑 |

每个专题页建议包含：

- 专题定位：解决什么问题。
- 适合读者：初级、中级、面试、实战、排障。
- 阅读路线：按顺序列出文章。
- 知识地图：列出核心概念之间的关系。
- 实践任务：给出可动手验证的小任务。
- 延伸阅读：官方文档、经典书籍、源码入口。

验收标准：

- 至少 2 个专题页达到可公开展示质量。
- 每个专题页至少关联 5 篇文章或明确待写文章。
- 首页能直接进入核心专题。

### P1：SEO 与站点可发现性

目标：提升搜索引擎收录质量和分享展示效果。

待办：

- 启用或补充 sitemap 配置，并确认站点 hostname 正确。
- 增加 `robots.txt`。
- 为首页、关于页、友链页补充明确描述。
- 为文章补齐 `description`，避免搜索结果截取正文首段不稳定。
- 统一 Open Graph 图片策略，保证 `og:image` 可访问且尺寸合适。
- 检查中文路径、空格文件名、括号文件名在构建和部署后的 URL 表现。
- 建议建立文章 slug 规范，降低中文文件名在外链传播中的编码问题。

验收标准：

- 发布后可访问 sitemap 和 robots。
- 分享首页和文章时标题、描述、图片展示正常。
- 主要文章 URL 稳定，不因重命名频繁变化。

### P1：订阅与复访机制

目标：借鉴长期技术博客的复访设计，让读者能持续跟踪更新。

待办：

- 确认 VuePress/theme-hope 是否启用 RSS feed 插件或主题内置 feed 能力。
- 在首页、关于页、页脚提供 RSS 入口。
- 增加“最近更新”或“更新日志”页面，记录新增文章和重要修订。
- 可以新增“每周技术摘录”或“开发记录”轻量栏目，用于稳定更新。
- 为重要文章增加“最后更新于”展示，降低技术内容过期风险。

验收标准：

- 读者可以通过 RSS 或更新日志跟踪新内容。
- 修改旧文章时能留下修订记录。
- 首页能体现站点仍在维护。

### P1：可信度与作者品牌

目标：提升技术博客的专业可信度，而不仅是文章堆叠。

待办：

- 完善 `docs/about.md`：技术方向、项目经历、写作原则、联系方式。
- 为文章增加“为什么写这篇”“解决过什么实际问题”。
- 对引用外部资料的文章增加参考链接，不只写个人理解。
- 对容易随版本变化的文章标注“适用版本”和“最后验证时间”。
- 对原创、转载、摘录建立明确规则。

验收标准：

- 关于页能说明作者背景和博客定位。
- 深度文章有来源、版本、验证过程。
- 读者可以判断文章是否仍适用于当前技术版本。

### P1：图片资产治理

目标：减少图片缺失、路径错误和外链不可用问题。

待办：

- 明确本地图片路径规范，建议统一放在 `docs/.vuepress/public/images/` 下按栏目分组。
- 补充 `.env.example` 中图床配置说明和必填项。
- 为 `upload-images.js`、`image-processor.js` 增加使用示例、失败处理说明。
- 增加图片引用扫描报告，区分本地图片、远程图片、缺失图片。
- 发布前运行图片检查，避免构建后图片 404。

验收标准：

- Markdown 中的图片链接可被脚本扫描。
- 缺失图片能在发布前被发现。
- 远程图床迁移不会破坏本地备份。

### P2：工程质量与依赖治理

目标：降低依赖升级和主题定制的维护成本。

待办：

- 记录当前 VuePress、theme-hope、Vue、Vite 相关版本组合。
- 建立依赖升级策略：小版本优先、RC 到 stable 单独验证。
- 增加 `npm run docs:build` 到 PR 或合并前检查。
- 考虑增加 Markdown lint 或自定义扫描脚本，检查标题层级、空链接、重复标题。
- 梳理现有根目录文档，减少过期说明：
  - `README.md`
  - `使用说明.md`
  - `OPTIMIZATION_GUIDE.md`
  - `FONT_OPTIMIZATION.md`
  - `RENAME_REPORT.md`

验收标准：

- 依赖升级前后有明确验证命令。
- 根目录文档职责清晰，不重复描述同一流程。
- 构建错误能通过文档快速定位到常见原因。

### P2：评论与互动

目标：让博客从静态阅读进一步支持反馈和交流。

待办：

- 确认是否启用 Giscus。
- 如果启用，补齐 repo、repoId、category、categoryId、mapping 等配置。
- 在隐私说明中说明第三方评论系统的使用。
- 确认深色模式下评论区域显示正常。

验收标准：

- 文章页评论区正常加载。
- 未配置评论时不出现异常占位或控制台错误。

### P2：体验与视觉基线

目标：在不大改主题的前提下，补齐成熟技术博客常见的阅读体验。

待办：

- 检查移动端导航、侧边栏、搜索、代码块横向滚动体验。
- 统一代码块语言标识，避免高亮失效。
- 为长文启用或确认目录锚点体验。
- 优化首页背景图体积，避免首屏加载慢。
- 检查深色模式下 logo、背景图、代码块、提示框的对比度。
- 为文章中的关键图示建立统一风格：流程图、时序图、内存结构图、架构图。

验收标准：

- 移动端可以顺畅阅读长文和代码块。
- 深色模式下没有低对比度文字。
- 首页首屏图片不显著拖慢加载。

### P2：内容质量评审标准

目标：建立发布前的编辑标准，减少低质量文章进入正式栏目。

建议每篇文章发布前自查：

- 标题是否准确表达问题，而不是泛泛而谈。
- 开头是否说明读者收益。
- 是否有核心结论。
- 是否有示例、图示或可复现步骤。
- 是否解释了常见误区。
- 是否标注版本和适用范围。
- 是否提供延伸阅读。
- 是否存在无来源的大段结论。
- 是否存在死链、缺图、标题层级跳跃。

验收标准：

- 正式文章都能通过上述清单。
- 临时笔记可以放在开发记录，但不直接进入核心专题。

## 4. 推荐近期执行顺序

1. 先跑通本地构建，确认当前未提交改动下是否可构建。
2. 核对导航链接和空目录，修复最容易造成 404 的入口。
3. 更新新文章模板，固定 frontmatter 标准。
4. 为核心栏目补齐 README 和阅读路径。
5. 先建立 JVM、并发、MySQL 这 3 个专题索引页。
6. 补充 SEO 基础文件、RSS 和 sitemap 配置。
7. 整理根目录文档，把重复内容合并到 README 或专题文档中。
8. 建立发布前检查清单，包括构建、图片、链接、frontmatter。

## 5. 发布前检查清单

每次发布前建议执行：

```bash
npm install --legacy-peer-deps
npm run docs:build
npm run scan-images
```

人工检查：

- 首页是否正常加载。
- 导航栏所有一级入口是否可访问。
- 新增文章是否出现在侧边栏和归档中。
- 新增图片是否能在生产路径访问。
- GitHub Actions 是否部署成功。
- 新文章是否符合文章结构模板。
- 新文章是否被加入对应专题页或栏目 README。
- 文章中的外部引用是否可访问。

## 6. 后续可新增的自动化脚本

建议新增以下脚本，逐步降低人工检查成本：

| 脚本 | 作用 |
| --- | --- |
| `check-frontmatter.js` | 扫描文章 frontmatter 是否完整、分类标签是否符合白名单。 |
| `check-links.js` | 扫描 Markdown 内部链接和图片链接是否存在。 |
| `check-empty-sections.js` | 扫描导航指向的目录是否为空或缺少 README。 |
| `generate-content-index.js` | 生成栏目文章索引，辅助规划内容路线。 |
| `prepublish-check.js` | 聚合构建、frontmatter、链接、图片检查。 |
| `check-article-quality.js` | 检查文章是否包含摘要、适用范围、核心结论、延伸阅读。 |
| `generate-series-index.js` | 从 frontmatter 或目录生成专题索引页草稿。 |

## 7. 维护原则

- 文章优先进入 `docs/posts/` 下的明确栏目，不把临时笔记堆在根目录。
- 新栏目必须同时补齐导航、栏目 README、至少一篇正文或明确的占位计划。
- 图片先保留本地备份，再考虑上传到图床。
- 生产发布以 `npm run docs:build` 通过为最低门槛。
- 主题配置变更应集中在 `docs/.vuepress/config.js`，避免散落修改。
- 依赖升级和大规模文章重命名应单独提交，便于回滚。
- 核心专题优先保证深度和连续性，轻量内容放在开发记录或周刊类栏目。
- 文章对外发布后尽量保持 URL 稳定，重命名前要评估外链影响。
- 涉及版本差异的技术内容必须标注验证版本和更新时间。

## 8. 参考标杆

以下站点用于本次分析时的方向参考：

- [Martin Fowler](https://martinfowler.com/)
- [阮一峰的网络日志](https://www.ruanyifeng.com/blog/)
- [GitHub Blog](https://github.blog/)
- [Vercel Blog](https://vercel.com/blog)
- [Stripe Engineering](https://stripe.com/blog/engineering)
- [Netflix TechBlog](https://netflixtechblog.com/)
- [web.dev](https://web.dev/)
- [MDN Learn](https://developer.mozilla.org/en-US/docs/Learn)
